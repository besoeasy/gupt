node_modules
.npm-cache
dist

# build outputs
build/
bundle/

# flatpak local build cache
.flatpak-builder/

# stray lockfile (project uses npm / package-lock.json)
bun.lock

# local audit notes (never committed)
audit.txt

# local claude/opencode skills/config
.claude/
